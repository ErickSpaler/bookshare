<div class="container mt-5">
  <div class="row">
    <div class="col-3">
    <% if @book.image.nil? %>
        <%= image_tag 'https://cdn-icons-png.flaticon.com/512/2232/2232688.png' %>
        <% else  %>
        <%= image_tag @book.image %>
        <% end %>
    </div>
    <div class="col-9">
      <h2>Book: <%= @book.title %></h2>
      <h4>Author Name: <%= @book.author_name %></h4>
      <h4>Description: <%= @book.description %></h4>
      <h4>Published Year: <%= @book.published_year %></h4>
      <h4>Genre: <%= @book.genre %></h4>
      <button class="btn btn-dark"><%= link_to "QUERO ALUGAR", my_titles_books_path %></button>
    </div>
  </div>

  <div class="mt-3">
  <h3>Price: R$10 </h3>
  </div>

  <div class="user-info d-flex flex-column bd-highlight mt-3 mb-3 border border-secondary">
    <h3>User Info</h3>
    <h4>Name: <%= @book.user.name %></h4>
    <h4>Address: <%= @book.user.address %></h4>
  </div>

  <%= link_to "Back to home", books_path, class: "btn btn-dark" %>
</div>
